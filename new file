

## 分析與建議

### 一、r5 obs/reward 重構：你做對的地方

先肯定已落地的改動在技術上是正確的：

**M1a POMDP 修復** 設計得很乾淨。`_compute_pomdp_features()` 同時被 `_get_observation()` 和 `_compute_reward()` 呼叫，確保 obs 裡的 `rolling_vol / sortino_proxy / current_dd` 與驅動 reward 的狀態完全一致，POMDP 問題確實被部分緩解。

**M1d softmax_temp 0.5→1.0** 也正確。原本 0.5 讓 softmax 過度尖銳，梯度幾乎只流向最高 logit 那個股票，造成集中度問題。1.0 讓梯度更均勻，有助學習多樣化配置。

**M1c entropy floor** 方向對，但有一個邊界效應需注意（見下方）。

---

### 二、r5 仍存在的三個問題

#### 問題 1：`sortino_component * capital_util` 反向激勵（高優先）

這是目前 reward 最微妙的 bug。看這段：

```python
sortino_component = sortino_proxy * capital_util
```

`capital_util = np.sum(np.abs(self._positions))` — 即股票曝險程度。

**後果**：當 agent 在回撤中想減倉（降低 capital_util），sortino_component 縮小，reward 反而下降。也就是說，「減倉以控制 MDD」這個正確行為，在 sortino 分量上是有懲罰的。這跟 LAMBDA_DRAWDOWN 的方向相反，兩者在深度回撤時互相抵消。

**建議修法**：拿掉 `capital_util` 乘數，或改用一個不懲罰減倉的版本：

```python
# 方案A：直接使用 sortino_proxy，不乘 capital_util
sortino_component = sortino_proxy

# 方案B：讓高曝險在正 sortino 時加分，但不懲罰減倉
sortino_component = sortino_proxy * max(capital_util, 0.3)
```

---

#### 問題 2：drawdown penalty 是線性流量，但 Gate 測的是端點峰值（高優先）

```python
drawdown_p = LAMBDA_DRAWDOWN * max(0.0, raw_dd - REWARD_REF_DD)
```

這個懲罰每天計收，對「平均每天的回撤深度」有效。但 Gate 測的是整個 OOS 區間的 `max_drawdown`，即最深的那一天。

**對策1：凸形懲罰（近 Gate 門檻急速放大）**

```python
# 在 28% 以下線性，超過 28% 開始加速
if raw_dd < 0.28:
    drawdown_p = LAMBDA_DRAWDOWN * max(0.0, raw_dd - REWARD_REF_DD)
else:
    drawdown_p = LAMBDA_DRAWDOWN * (0.26 + 3.0 * (raw_dd - 0.28) ** 2)
```

這讓 agent 在接近 Gate 門檻（35%）前就感受到急劇增強的懲罰，而不是線性外推。

**對策2：對 `_max_drawdown` 追加 episode-level 終止獎懲**

目前 obs 有 `IDX_MAX_DRAWDOWN`，但 reward 對歷史最大回撤沒有額外懲罰。agent 在 episode 早期跌了 40% 再回來，整段累積 reward 可能還是正的。

建議在 `step()` 的 terminated 判斷加一個條件性終止：

```python
# 在 step() 的 terminated 計算旁
mdd_breach = self._max_drawdown > 0.38  # 軟上限，比 Gate 35% 留一點空間
if mdd_breach:
    # 大額負 reward 並終止 episode
    reward = -1.0
    terminated = True
```

這讓 agent 把「不超過 38% MDD」當成生存條件，而不只是每日流量的加減項。

---

#### 問題 3：M1c entropy floor 可能壓縮 cash-defensive 空間（中優先）

```python
stock_weights[topk_indices] = np.maximum(stock_weights[topk_indices], MIN_TOP_K_WEIGHT)
total = float(np.sum(stock_weights) + cash_weight)
# normalize...
```

`topk=5`、`MIN_TOP_K_WEIGHT=0.05`，意味著股票最低配置恆為 25%，**cash 上限永遠 ≤ 75%**。

在嚴重回撤時，agent 如果想「幾乎全現金」是做不到的，這跟 `cash_defensive_bonus` 的設計目的矛盾。

**建議**：讓 floor 與 drawdown 狀態掛鉤：

```python
# 在深度回撤時允許更大比例的現金
effective_floor = MIN_TOP_K_WEIGHT if raw_dd < 0.15 else 0.01
stock_weights[topk_indices] = np.maximum(stock_weights[topk_indices], effective_floor)
```

或者更簡單，乾脆把 floor 從 5% 調低到 2%，讓 softmax 本身決定集中度，entropy floor 只用來防止單點 99% 極端集中。

---

### 三、Gate 阻塞的根本解法路徑圖

以下是三條路，依成功機率排序：

```
路徑 A（r5 微調 — 最快）
────────────────────────
1. 修掉 sortino * capital_util 反向激勵
2. 換凸形 drawdown penalty
3. 加 MDD-breach episode termination
預期：worst-case MDD 可能降 3–6%，有機會從 44% 降到 38% 以下

路徑 B（M3-1 episode 設計 — 中期）
──────────────────────────────────
固定長度 episode（如 126 交易日 = 6 個月）+ 隨機起點
理由：整段歷史為單一 episode 讓 credit assignment 橫跨牛熊，
agent 在熊市跌深後仍有半年可以「彌補」，不需要急著控制 MDD。
切成固定短 episode + 終止懲罰，直接把 MDD 問題的時間尺度
縮短到 6 個月，與 Gate 的測量週期對齊。

路徑 C（SL hybrid — 保底選項）
──────────────────────────────
SL（LightGBM）Sortino 2.53，MDD 38.55%，比 RL 更穩定。
從 38.55% → 35% 的差距（3.5%）比 RL 的 44% → 35%（9%）小得多。
實際上只需要在 SL 層疊加一個 capital-at-risk 規則：
當 rolling MDD > 28% 時強制降曝險 50%（trade_guard 層實現）。
這不需要重訓模型，只需一個外層 guard。
```

**建議執行順序**：先做路徑 A（一天內可完成），跑 M2-smoke 看效果。如果 MDD 方向改善但未達標，再評估 B 或 C，不要同時開多條路。

---

### 四、可以馬上做的程式碼修改

針對 `trading_env.py`，以下是三個最小侵入的改動，不改大架構、可單變因驗證：

**改動 1：reward 中移除 capital_util**（`_compute_reward`）

```python
# 原本
sortino_component = sortino_proxy * capital_util
# 改為
sortino_component = sortino_proxy
```

**改動 2：換凸形 drawdown penalty**（`_compute_reward`）

```python
# 原本
drawdown_p = LAMBDA_DRAWDOWN * max(0.0, raw_dd - REWARD_REF_DD)
# 改為
excess = max(0.0, raw_dd - REWARD_REF_DD)
drawdown_p = LAMBDA_DRAWDOWN * (excess + 2.0 * max(0.0, raw_dd - 0.25) ** 2)
```

**改動 3：加 episode termination on MDD breach**（`step()`）

```python
mdd_breach = bool(self._max_drawdown > 0.38)
terminated = self._current_step >= self.max_steps - 1

if mdd_breach and not terminated:
    terminated = True
    reward = -1.0  # override reward
    return self._get_observation(), reward, terminated, False, self._get_info()
```

這三個改動都是單變因，建議一次只上一個，smoke 驗收後再疊加，符合你們 v3 的研究紀律。