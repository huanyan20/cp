# 重構計畫 - 現況評估與下一階段

最後更新：2026-06-08 21:35 後

## 總結

P0-P6 重構已完成，N1 正式 walk-forward 訓練與 N5 with-features 補訓也已完成。最新 Promotion Gate 仍未通過，阻擋點已從「資料未齊」轉為「模型品質與報告分組」：

- `ppo_enabled_with_features` 已有 seed42/43/44 完整 metrics、model、chart。
- `p5_analysis.py` 已重跑 `ablation`、`stress`、`baseline`。
- `experiment_report.py` 已重跑並產出最新 `experiment_report.md` / `experiment_summary.json`。
- 當前 Promotion Gate：`BLOCKED`，6/8 gates passed。
- 主要失敗：Drawdown Gate、Ablation (Features)。

## 已完成項目

| 項目 | 狀態 | 說明 |
|---|---|---|
| P0 live guard | 完成 | `trade_guard.py`：TTL / AID / macro / risk limit guard |
| P1 測試與 lint | 完成 | 測試基線與 `ruff` 已建立 |
| P2 settings | 完成 | `ResearchSettings` / `EvaluationSettings` / `LiveSettings` / `PathSettings` / `RiskLimits` |
| P3 data pipeline | 完成 | `data_pipeline/` 拆分 market fetch / features / enrichment |
| P4 walk-forward | 完成 | `research_pipeline.py` 拆分 period planning / train-eval / metrics |
| P5 experiment report | 完成 | `promotion_gate.py` + `p5_analysis.py` |
| P6 cmoney_rpa | 完成 | `cmoney_client.py` / `signal_validator.py` / `rebalance_planner.py` |
| N1 300K 訓練 | 完成 | PPO/SAC x cash variants 的主要矩陣已具備 3 seeds |
| N5 with_features 補訓 | 完成 | `ppo_enabled_with_features` seed42/43/44 已齊 |
| walk-forward resume | 完成 | period model 已存在時可跳過訓練並補 evaluation |

## 最新 Promotion Gate

```text
Promotion Result: BLOCKED
Risk Level: MEDIUM
Passed: 6/8 gates
Failed: Drawdown Gate, Ablation (Features)
```

目前最佳候選由 report ranking 選為：

```text
SAC / cash=enabled
Sortino: 2.32 +/- 0.17
Max Drawdown: 36.09% +/- 2.62%
Worst-case MDD: 38.71%
Limit: 35.00%
```

因此尚不可 promote。

## Ablation 結論

`ppo_enabled_with_features` 三個 seed 已補齊，但 overnight features 不應直接成為預設輸入：

| 指標 | With Features | Without Features | Delta |
|---|---:|---:|---:|
| Sortino | 0.87598 | 2.20340 | -1.32742 |
| Total Return | 1.079762 | 1.592630 | -0.512868 |
| Max Drawdown | 0.254605 | 0.361223 | -0.106618 |
| Turnover | 0.004470 | 0.031467 | -0.026997 |

解讀：overnight features 改善 drawdown 與 turnover，但明顯犧牲 Sortino、總報酬與勝率。此特徵目前只能視為風險抑制訊號，不能視為提升 alpha 的預設特徵。

## 報告分組修正

`experiment_report.py` 原本只用 `algo + cash_mode` 分組，會把 `ppo_enabled` 與 `ppo_enabled_with_features` 混在同一個 summary row。這會讓主排名、seed dispersion 與 period breakdown 不夠乾淨。

已實作：

1. metrics reader 保留 `variant`：`base` / `with_features`。
2. walk-forward summary 依 `algo + cash_mode + variant` 分組。
3. period breakdown 同樣依 `algo + cash_mode + variant` 分組。
4. Promotion Gate 的 period consistency 只評估最佳候選 variant。
5. 已重跑 `python experiment_report.py`，最新 summary 不再混合 feature variants。

修正後 ranking：

| Rank | Model | Variant | Sortino | MDD | Total Return |
|---:|---|---|---:|---:|---:|
| 1 | SAC enabled | base | 2.32 +/- 0.17 | 36.09% +/- 2.62% | 163.70% +/- 20.14% |
| 2 | PPO disabled | base | 2.29 +/- 0.63 | 34.94% +/- 3.97% | 209.94% +/- 113.45% |
| 3 | PPO enabled | base | 2.20 +/- 0.45 | 36.12% +/- 2.61% | 159.26% +/- 63.01% |
| 4 | SAC disabled | base | 1.94 +/- 0.23 | 40.11% +/- 3.93% | 127.72% +/- 20.54% |
| 5 | PPO enabled | with_features | 0.88 +/- 1.10 | 25.46% +/- 12.56% | 107.98% +/- 155.28% |

## 下一階段優先順序

| 優先級 | 工作 | 目的 |
|---|---|---|
| R1 | 修正 experiment report variant 分組 | 已完成 |
| R2 | 重跑 `experiment_report.py` | 已完成 |
| R3 | 檢查最佳候選是否仍卡 MDD | 已確認仍失敗 |
| R4 | 若 MDD 仍失敗，調整 reward 或 exposure cap | 將 worst-case MDD 壓回 35% 內 |
| R5 | 暫停將 overnight features 設為預設 | 等特徵品質改善後再重新 ablation |

## 建議決策

短期不要上線 promote。理由：

- 最佳候選的 worst-case MDD 仍高於 35%。
- overnight features 的 ablation 對 Sortino 是負貢獻。
- 2026H1 對部分 seed/模型貢獻過大，仍有 period concentration 風險。

下一個研究迭代應優先降低 MDD，而不是繼續補同設定 seeds。可考慮：

- 加重 `trading_env.py` 的 drawdown penalty。
- 降低單一持股集中度或調整 top-k。
- 在高 drawdown regime 增加現金曝險誘因。
- 將 overnight features 改為 risk overlay，而不是直接拼入 alpha observation。

## 驗收指令

```bash
python -m compileall -q -x env experiment_report.py promotion_gate.py
python -m unittest tests.test_experiment_report -v
python -m unittest tests.test_promotion_gate -v
ruff check experiment_report.py promotion_gate.py tests/test_experiment_report.py
python experiment_report.py
```
